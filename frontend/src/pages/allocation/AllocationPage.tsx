import { Container, Typography } from '@mui/material';

const AllocationPage = () => {
  return (
    <Container maxWidth="xl">
      <Typography variant="h4" gutterBottom>
        Resource Allocation
      </Typography>
      {/* TODO: Implement allocation board with drag-and-drop */}
    </Container>
  );
};

export default AllocationPage;
